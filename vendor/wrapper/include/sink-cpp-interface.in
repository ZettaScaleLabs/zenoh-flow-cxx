    pub struct Input {
        pub data: Vec<u8>,
        pub timestamp: u64,
    }

    unsafe extern "C++" {
        include!("sink.hpp");

        type State;

        fn initialize(configuration: &Vec<Configuration>) -> UniquePtr<State>;

        fn run(
            context: &mut Context,
            state: &mut UniquePtr<State>,
            input: Input,
        ) -> Result<()>;
    }
}
